version: '3.9'

networks:
  sibiriana-net:
    external: true

services:
  predict:
    container_name: predict
    image: predict
    build: .
    ports: 
      - 1234:1234
    networks:
      - sibiriana-net